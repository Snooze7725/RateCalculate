# Rate Calculate

With this mod, you can select an area (default `C` on PC, and the `select` button on mobile) and calculate the following: drilling speed, factory production, factory consumption, and pumping speed.
